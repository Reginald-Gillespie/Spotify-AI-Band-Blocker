{
  "name": "ai_band_blocker",
  "version": "1.0.0",
  "description": "Spicetify extension to block AI bands",
  "private": false,
  "scripts": {
    "inject-hard": "npm run build && spicetify config extensions ai_band_blocker.js && spicetify apply",
    "inject": "spicetify-creator && spicetify apply",
    "watch": "spicetify-creator --watch",
    "build": "spicetify-creator --out=dist --minify"
  },
  "author": "WKoA",
  "license": "MIT",
  "devDependencies": {
    "@types/react": "^19.2.6",
    "@types/react-dom": "^19.2.3",
    "spicetify-creator": "^1.0.17"
  }
}